### ssh-config

Parameters:
- --no-root-login == убрать возможность логина под рута 

```bash
sudo bash ssh-config.sh 1234 --no-root-login
```


### docker_install

```bash
sudo bash docker_install.sh
```
